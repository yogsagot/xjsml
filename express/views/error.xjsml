  <h1> <echo message /></h1>
  <if error.status><h2> <echo error.status /> </h2></if>
  <h3><if error.fileName> in file: <echo error.fileName /></if>
  <if error.lineNumber> on line: <echo error.lineNumber /></if></h3>
  <pre> <echo error.stack /> </pre>

