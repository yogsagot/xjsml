<div class=["one", params.required ? "required" : "not-required", `1three${params.id}`] sort style=params sort >
<comment><if (arr[0] > 0) ></comment>

<with user>
    <echo firstname /> <echo lastname />
</with>

<comment></if></comment>

</div>